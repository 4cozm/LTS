#.git/hook 에 넣어주세요

echo "▶ Running dotnet format..."
dotnet format ../LTS/LTS.csproj --verify-no-changes

if [ $? -ne 0 ]; then
  echo "❌ Formatting check failed. Please run 'dotnet format' before committing."
  exit 1
fi

echo "✅ Formatting OK. Proceeding with commit."